#include <stdio.h>

int main(void)
{
    int x, y;
     
    x = +10 + +1; // números inteiros positivos
    y = -10 - -1; // números inteiros negativos

    printf("X: %d\nY:%d\n", x, y);

    return 0;
}